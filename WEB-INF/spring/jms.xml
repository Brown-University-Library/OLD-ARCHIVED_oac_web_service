<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:amq="http://activemq.apache.org/schema/core"
       xmlns:jms="http://www.springframework.org/schema/jms"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans 
       http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context-3.0.xsd
       http://activemq.apache.org/schema/core
       http://activemq.apache.org/schema/core/activemq-core.xsd
       http://www.springframework.org/schema/jms 
       http://www.springframework.org/schema/jms/spring-jms.xsd
       ">
  
    <!-- setup an embedded JMS Broker -->
    <amq:broker id="amq.broker" useJmx="false" persistent="false" brokerName="localhost">
      <amq:transportConnectors>
        <amq:transportConnector uri="tcp://localhost:61616" />
      </amq:transportConnectors>
    </amq:broker>

    <amq:connectionFactory id="jmsFactory" brokerURL="vm://localhost"/>

    <amq:topic id="destination" physicalName="fedora.apim.update"/>

    <bean id="consumer.messageListener" class="MessageListenerImpl" />

    <bean id="jmsConsumerConnectionFactory" 
          class="org.springframework.jms.connection.SingleConnectionFactory"
          depends-on="amq.broker"
          p:targetConnectionFactory-ref="jmsFactory" />

    <bean id="local.transactionManager"
          class="org.springframework.jms.connection.JmsTransactionManager"
          depends-on="amq.broker">
      <property name="connectionFactory" ref="jmsConsumerConnectionFactory" />
    </bean>

    <jms:listener-container container-type="default" 
                            connection-factory="jmsConsumerConnectionFactory"
                            acknowledge="auto"
                            destination-type="topic">
        <jms:listener destination="fedora.apim.update" ref="consumer.messageListener" />
    </jms:listener-container>

    <!--amq:broker id="amq.broker" useJmx="false" persistent="false" brokerName="localhost">
      <amq:transportConnectors>
        <amq:transportConnector uri="tcp://localhost:61616" />
      </amq:transportConnectors>
    </amq:broker>

    <bean id="amq.connectionFactory"
          class="org.apache.activemq.ActiveMQConnectionFactory"
          depends-on="amq.broker">
      <property name="brokerURL"
                value="vm://localhost" />
    </bean>

    <bean id="consumer.connectionFactory"
          class="org.springframework.jms.connection.CachingConnectionFactory">
      <constructor-arg ref="amq.connectionFactory" />
    </bean>

    <bean id="consumer.messageListener"
          class="oac.jms.listener.MessageListenerImpl" />

    <bean id="local.transactionManager"
          class="org.springframework.jms.connection.JmsTransactionManager"
          depends-on="amq.broker">
      <property name="connectionFactory" ref="consumer.connectionFactory" />
    </bean>

    <bean id="jmsContainer"
          class="org.springframework.jms.listener.DefaultMessageListenerContainer">
      <property name="connectionFactory" ref="consumer.connectionFactory" />
      <property name="destinationName" value="topic:fedora.apim.update" />
      <property name="messageListener" ref="consumer.messageListener" />
      <property name="transactionManager" ref="local.transactionManager" />
    </bean-->

</beans>